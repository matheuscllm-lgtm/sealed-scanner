[pytest]
# The pytest suite lives in tests/. Root-level probe_*.py / run_*.py and the
# scripts/ helpers are standalone run-scripts, not pytest tests — scoping
# collection here keeps `python -m pytest` fast and clean.
testpaths = tests
